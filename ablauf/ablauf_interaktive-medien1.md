
# Ablauf: Interaktive Medien 1 – Technischer Teil

Dieses Dokument beschreibt den schrittweisen Aufbau einer einfachen Webstruktur anhand von **example1**. Die Übungen führen von HTML-Grundlagen über CSS-Verknüpfung bis zur Integration einer JavaScript-Datei.

---

## 1. HTML: Aufbau und Grundstruktur

**Ziele:**
- Die Studierenden verstehen die Grundstruktur einer HTML-Datei.
- Sie können semantische HTML-Elemente nutzen.

**Inhalte:**
- HTML-Skelett (`<!DOCTYPE html>`, `<html>`, `<head>`, `<body>`)
- Wichtige **Elemente**: `<h1>`, `<p>`, `<a>`, `<img>`, `<div>` ...
- **Attribute** wie `src`, `alt`, `href`, `class`, `id`
- Unterschiede und Anwendung von **Klassen** (`class`) und **IDs** (`id`)

**Aufgabe:**
- Studierende erstellen eine eigene HTML-Datei mit mindestens:
  - einer Überschrift,
  - einem Absatz,
  - einem Bild,
  - einem Link,
  - und mindestens einer Klasse und einer ID.

---

## 2. CSS: Gestaltung und Verknüpfung

**Ziele:**
- Verstehen, wie CSS-Dateien mit HTML verbunden werden.
- Grundlagen der Formatierung mit CSS anwenden können.

**Inhalte:**
- `<link rel="stylesheet" href="style.css">`
- **Selektoren**: Element-, Klassen- und ID-Selektoren
- **Eigenschaften**: `color`, `background`, `padding`, `margin`, `font-size`, `border`, ...

**Aufgabe:**
- Studierende erstellen eine eigene CSS-Datei.
- Sie verknüpfen diese mit ihrer HTML-Datei.
- Sie gestalten Überschrift, Absatz und Link mit unterschiedlichen Selektoren.

---

## 3. JavaScript: Erste Schritte

**Ziele:**
- Die Studierenden verstehen, wie JavaScript eingebunden wird.
- Sie können einfache JS-Ausgaben in der Konsole erzeugen.

**Inhalte:**
- `<script src="script.js"></script>`
- Erklärung der Datei „script.js“
- Nutzung von `console.log("Nachricht")` zur Ausgabe in der Entwicklertools-Konsole

**Aufgabe:**
- Studierende erstellen eine eigene JS-Datei.
- Sie verknüpfen diese mit ihrer HTML-Datei.
- Sie schreiben ein eigenes `console.log(...)` und überprüfen die Ausgabe in den DevTools.

---

# 🎬 Drehbücher für Tutorial-Videos

## 🎥 Tutorial 1: HTML-Grundstruktur und Elemente

**Titel:** „HTML verstehen – Struktur, Elemente und Attribute“

**Kapitel:**
1. Intro: Was ist HTML?
2. HTML-Grundstruktur Schritt für Schritt
3. Typische Elemente (Überschrift, Absatz, Link, Bild, div)
4. Klassen und IDs: Wann verwende ich was?
5. Aufgabenstellung an Studierende

---

## 🎥 Tutorial 2: CSS einbinden und gestalten

**Titel:** „Webseiten gestalten mit CSS – Farben, Schriften, Layout“

**Kapitel:**
1. Verlinkung einer CSS-Datei
2. Unterschied: Element-, Klassen-, ID-Selektoren
3. Erste Eigenschaften: `color`, `background`, `padding` ...
4. Live-Style-Änderung und Tipps zur Struktur
5. Aufgabenstellung: Erste Gestaltung umsetzen

---

## 🎥 Tutorial 3: Einstieg in JavaScript

**Titel:** „Erste Schritte mit JavaScript – Die Konsole nutzen“

**Kapitel:**
1. JS-Datei erstellen und verlinken
2. console.log und Debugging in der Konsole
3. Typische Fehlerquellen (Dateipfad, Platzierung im HTML)
4. Aufgabenstellung: Eigene Ausgabe schreiben und testen

---

Alle Schritte bauen aufeinander auf und führen am Ende zu einer kleinen, interaktiven Website.
